[build]
builder = "DOCKERFILE"

# Force immediate redeployment for missing endpoints
# Build timestamp: 2026-04-21T19:42:00Z
# Deploy: /api/dashboard/stats, /api/dashboard/simple, /api/events, /api/fix-created-by
