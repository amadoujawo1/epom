[build]
builder = "DOCKERFILE"

# Force complete rebuild - AGGRESSIVE ROLES FIX
# Build timestamp: 2025-04-25T22:30:00Z
# Issue: Railway serving cached frontend with old roles
# Solution: Complete rebuild with aggressive cache-busting

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
